build:
  box: nodesource/trusty
  steps:
    - npm-install
deploy:
  box: phusion/baseimage
  steps:
    - script:
      name: ls
      code: |-
        ls -la
    - adminhengroen/deploy-to-ebs:
      key-id: $AWS_ACCESS_KEY_ID
      key-secret: $AWS_SECRET_KEY
      region: $AWS_REGION
      app-version: $WERCKER_GIT_COMMIT