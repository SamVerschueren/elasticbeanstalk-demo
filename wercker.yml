box: nodesource/trusty
build:
  steps:
    - npm-install
deploy:
  steps:
    - koding/eb-deploy:
      access-key: $AWS_KEY
      secret-key: $AWS_SECRET
      app-name: eb-nodejs
      env-name: eb-nodejs-dev
      region: $AWS_REGION
      version-label: $WERCKER_GIT_COMMIT